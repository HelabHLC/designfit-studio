# Public hostname resolved to the IONOS server.
DESIGNFIT_DOMAIN=designfit.example.com

# Runtime classification exposed by the health endpoint.
APP_ENV=production

# Immutable release identifier, normally the Git commit SHA.
APP_VERSION=local
